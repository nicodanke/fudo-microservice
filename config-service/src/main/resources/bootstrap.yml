server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: ruta al repo
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      namme: root
      password: secret