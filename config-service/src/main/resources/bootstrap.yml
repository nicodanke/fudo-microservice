server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nicodanke/fudo-microservice.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      namme: root
      password: secret